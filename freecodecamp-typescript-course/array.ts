const superHeros: string[] = [];
// const heroPower: number[] = [];
const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

const MLModels: number[][] = [[255, 255, 255], []];

superHeros.push("spiderman");
heroPower.push(2);
MLModels.push([12, 12, 12]);

allUsers.push({ name: "", isActive: true });

export {};
